-- ───────────────────────────────────────────────────────────
-- 전문가 약력(강사소개 ✓ 불릿) — 직접 편집 가능하도록 컬럼 추가. 재실행 안전.
-- 비어 있으면 상세페이지는 기존 하드코딩(EXPERT_CREDENTIALS)으로 폴백.
-- ───────────────────────────────────────────────────────────
alter table experts add column if not exists credentials text[] not null default '{}';
